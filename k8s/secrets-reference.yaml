# =============================================================================
# K8s Secrets — created by minikube-setup.sh from your .env file
# DO NOT commit real secrets to Git. This file is a reference only.
# The setup script creates these secrets imperatively via kubectl.
# =============================================================================

# --- Database secret (example structure — values injected by setup script) ---
# kubectl create secret generic cicd-db-secret \
#   --from-literal=POSTGRES_DB=cicd_dashboard \
#   --from-literal=POSTGRES_USER=cicd_user \
#   --from-literal=POSTGRES_PASSWORD=<your-password> \
#   --namespace=cicd-dashboard

# --- Application secret (example structure — values injected by setup script) ---
# kubectl create secret generic cicd-app-secret \
#   --from-literal=GITHUB_TOKEN=<your-token> \
#   --from-literal=GITHUB_OWNER=<your-owner> \
#   --from-literal=GITHUB_REPO=<your-repo> \
#   --namespace=cicd-dashboard
